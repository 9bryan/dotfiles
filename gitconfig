[core]
    user = Kai Pak
    email = kai@kaipak.org
    editor = vim
    autocrlf = input
    pager = less -FRSX
[color]
    branch = auto
    diff = auto
    interactive = auto
    pager = true
    status = auto
    ui = true
[color "status"]
    added = green
    changed = yellow
    untracked = red
[push]
    default = current
[alias]
    ci = commit
    co = checkout
    st = status -sb
    hist = log --graph --pretty=format:'%C(yellow)%h%C(auto)%d%Creset %s %C(white)- %an, %ar%Creset'
